foo {
  bar
}

macro my_macro [ wololo ]

foo ${my_macro} "bar"

  macro internal [
    ${1} bar
  ]

macro my_macro2 [
  with ${1} "value"
  ignore # comment
  second "arg" ${2}

  ${internal "wow"}
]

foo2 ${my_macro "name" "\"string\""} "bar2"

macro my_macro [ wololo ${1} ]

override ${my_macro "foo"} "bar"
